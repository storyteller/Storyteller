<UserControl x:Class="StoryTeller.UserInterface.Controls.TestStopConditionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="{DynamicResource SelectedBackgroundBrush}" >
    <Border BorderBrush="Black" BorderThickness="2" Margin="5" Padding="5">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
        <Label FontWeight="Bold" Foreground="Black" Margin="0 0 0 5">Test Execution Options:  </Label>
            <CheckBox Name="wrongs"  Foreground="Black" Margin="5 5 0 5">Stop on the first failure</CheckBox>
            <CheckBox Name="exceptions" Foreground="Black" Margin="5 5 0 5">Stop on the first exception</CheckBox>
        <StackPanel Orientation="Horizontal" Margin="0 2 0 5">
                <Label Margin="5,0" Foreground="Black" VerticalAlignment="Center">Timeout in seconds</Label>
            <TextBox Name="timeout" Width="40" VerticalAlignment="Center" BorderBrush="Black" Margin="0,0,10,0">30</TextBox>
        </StackPanel>
    </StackPanel>
        </Border>
</UserControl>
